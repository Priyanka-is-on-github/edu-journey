import logo from "/logo.svg";

const Logo = () => {
  // return <img height={130} width={130} alt="logo" src={logo} />;
  return <p className="text-xl font-bold text-sky-700">Edu Journey</p>
};

export default Logo;
